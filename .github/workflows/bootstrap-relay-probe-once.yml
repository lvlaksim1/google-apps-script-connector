name: Bootstrap Relay Probe Once

on:
  push:
    branches: [main]
    paths:
      - '.bootstrap-relay-probe'

permissions:
  contents: write

jobs:
  bootstrap:
    uses: ./.github/workflows/apps-script.yml
    with:
      project: relay-probe
      operation: bootstrap_deploy
    secrets: inherit
